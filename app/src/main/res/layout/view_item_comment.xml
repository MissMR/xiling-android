<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="android.view.View"/>

        <variable
            name="item"
            type="com.xiling.module.community.Comment"/>
    </data>

    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:orientation="vertical">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/ivAvatar"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginLeft="15dp"
            android:layout_marginRight="10dp"
            android:layout_marginTop="15dp"
            android:src="@drawable/img_default"
            app:imageUrl="@{item.headImage}"
            />

        <TextView
            android:id="@+id/tvTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_alignTop="@id/ivAvatar"
            android:layout_marginRight="15dp"
            android:text="@{item.getPostTime()}"
            android:textColor="@color/text_gray"
            android:textSize="@dimen/text_normal"/>

        <TextView
            android:id="@+id/tvName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignTop="@id/ivAvatar"
            android:layout_marginRight="10dp"
            android:layout_toLeftOf="@id/tvTime"
            android:layout_toRightOf="@id/ivAvatar"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{item.nickName}"
            android:textColor="@color/text_gray"
            android:textSize="@dimen/text_normal"/>

        <TextView
            android:id="@+id/tvContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvName"
            android:layout_marginBottom="8dp"
            android:layout_marginRight="10dp"
            android:layout_marginTop="8dp"
            android:layout_toRightOf="@id/ivAvatar"
            android:text="@{item.content}"
            android:textColor="@color/text_black"
            android:textSize="@dimen/text_normal"/>

        <View
            android:layout_width="match_parent"
            android:layout_height="0.5dp"
            android:layout_below="@id/tvContent"
            android:layout_marginLeft="15dp"
            android:layout_marginTop="7dp"
            android:background="@color/line_color"
            app:isGone="@{item.hasReply()}"
            />

        <LinearLayout
            android:id="@+id/llReply"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvContent"
            android:layout_toRightOf="@id/ivAvatar"
            android:orientation="vertical"
            app:isGone="@{item.hasReply()}"
            >

            <View
                android:layout_width="match_parent"
                android:layout_height="0.5dp"
                android:layout_below="@id/tvContent"
                android:background="@color/line_color"/>

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                >

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="主人回复:"
                    android:textColor="@color/text_gray"
                    android:textSize="@dimen/text_normal"/>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignTop="@id/ivAvatar"
                    android:layout_gravity="right"
                    android:layout_marginRight="15dp"
                    android:text="@{item.getReplyTime()}"
                    android:textColor="@color/text_gray"
                    android:textSize="@dimen/text_normal"/>

            </FrameLayout>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="10dp"
                android:layout_marginRight="10dp"
                android:layout_marginTop="7dp"
                android:text="@{item.reply}"
                android:textColor="@color/text_black"
                android:textSize="@dimen/text_normal"/>

        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="0.5dp"
            android:layout_below="@id/llReply"
            android:layout_marginLeft="15dp"
            android:background="@color/line_color"
            app:show="@{item.hasReply()}"
            />

    </RelativeLayout>
</layout>