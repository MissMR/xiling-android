<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fl_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ddm_gray_background"
    tools:context="com.xiling.ddui.activity.AuthActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/iv_info"
            android:layout_width="wrap_content"
            android:layout_height="170dp"
            android:src="@mipmap/security_bg_red" />

        <TextView
            android:id="@+id/tv_tip_security"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="安全问题已设置~"
            android:textSize="12dp"
            android:visibility="gone"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/tv_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="您还没有添加实名认证信息哦~"
            android:textColor="@color/ddm_gray"
            android:textSize="12dp" />


        <TextView
            android:id="@+id/tv_btn_add_question"
            style="@style/MyButton.DDMRed.Middle"
            android:layout_width="wrap_content"
            android:layout_marginTop="20dp"
            android:paddingLeft="20dp"
            android:paddingRight="20dp"
            android:text="添加安全问题" />

    </LinearLayout>

    <TextView
        android:id="@+id/tv_btn_update_question"
        style="@style/MyButton.DDMRed"
        android:layout_gravity="bottom"
        android:layout_marginBottom="30dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:text="修改安全问题" />
</FrameLayout>